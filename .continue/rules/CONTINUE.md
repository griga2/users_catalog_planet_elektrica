"# Worker Catalog - Project Guide\n\n## 1. Project Overview\n\n**Worker Catalog** is a Vue 3 single-page application for managing workers, roles, and organizational structures. It uses a modern tech stack with Pinia for state management, Vue Router for navigation, and Naive UI for the UI framework.\n\n### Key Technologies\n- **Vue 3.4.29** - Progressive JavaScript framework\n- **Vite 5.3.1** - Next generation frontend tooling\n- **Pinia 2.1.7** - Vue store for state management\n- **Vue Router 4.3.3** - Official router for Vue.js\n- **Naive UI 2.40.3** - Vue 3 UI component library\n- **Axios 1.7.7** - HTTP client for API requests\n\n### Architecture\n```\nProject Root\n├── src/\n│   ├── components/       # Reusable Vue components\n│   ├── views/           # Page-level components\n│   ├── stores/          # Pinia state stores\n│   ├── router/          # Vue Router configuration\n│   ├── api/             # API client and endpoints\n│   ├── middleware/      # API middleware (ratelimit, csrf)\n│   ├── utils/           # Utility functions\n│   └── main.js          # Application entry point\n├── openspec/            # Specification and change management\n│   ├── specs/           # Technical specifications\n│   └── changes/         # Implementation plans\n└── .continue/rules/     # Continue AI rules and documentation\n```\n\n## 2. Getting Started\n\n### Prerequisites\n- Node.js 16+ and npm\n- Code editor (VS Code recommended with Volar extension)\n- Git\n\n### Installation\n\n```bash\n# Install dependencies\nnpm install\n\n# Start development server\nnpm run dev\n\n# Build for production\nnpm run build\n\n# Preview production build\nnpm run preview\n\n# Lint code\nnpm run lint\n\n# Format code\nnpm run format\n```\n\n### Environment Variables\nCreate a `.env` file in the project root:\n\n```env\nVITE_SERVER_HOST=http://localhost:3000\n```\n\n### Running Tests\nNo test framework is currently configured. Add testing with:\n- **Vitest** for unit tests\n- **Testing Library** for Vue component tests\n\n## 3. Project Structure\n\n### Main Directories\n\n#### `src/components/` - Reusable Components\n- **LoginModel.vue** - Login modal dialog\n- **WorkTimeComponent.vue** - Work time tracking\n- **EditTimeComponent.vue** - Edit work time form\n- **CatalogRow.vue** - Catalog row display\n- **BranchTree.vue** - Organizational tree structure\n\n#### `src/views/` - Page Components\n- **LayoutView.vue** - Main layout with header and navigation\n- **WorkerCatalogView.vue** - Worker listing page\n- **RolesView.vue** - Role management page\n- **OrgStructView.vue** - Organizational structure page\n- **EditUsersView.vue** - User editing page\n\n#### `src/stores/` - Pinia State Management\n- **user.js** - User authentication and profile state\n- **counter.js** - Counter example store\n- **index.js** - Store exports\n\n#### `src/router/` - Navigation\n- **index.js** - Router configuration with routes\n\n#### `src/api/` - API Layer\n- **client.js** - Axios instance configuration\n\n#### `src/middleware/` - API Middleware\n- **ratelimit.js** - Rate limiting middleware\n- **csrf.js** - CSRF protection middleware\n\n#### `src/utils/` - Utilities\n- **sanitize.js** - Input sanitization\n- **debug.js** - Debug logging utility\n\n### Key Configuration Files\n\n| File | Purpose |\n|------|--------|\n| `vite.config.js` | Vite build configuration |\n| `jsconfig.json` | JavaScript configuration |\n| `.prettierrc.json` | Prettier formatting rules |\n| `package.json` | Dependencies and scripts |\n\n## 4. Development Workflow\n\n### Coding Standards\n\n#### Vue Components\n- Use `<script setup>` syntax\n- Use `<template>` for markup\n- Use `<style scoped>` for scoped CSS\n- Follow Vue 3 Composition API patterns\n\n#### Pinia Stores\n- Use `defineStore()` for store definition\n- Return computed values and methods\n- Use `ref`, `reactive` for state\n\n#### API Calls\n- Use `apiClient` from `@/api/client`\n- Handle errors consistently\n- Use middleware for auth/csrf\n\n### Testing Approach\nNo tests are currently configured. Recommended approach:\n1. Write unit tests for stores and utilities\n2. Write component tests with Testing Library\n3. Write integration tests for API calls\n\n### Build Process\n1. **Development**: `npm run dev` - Hot reload with Vite\n2. **Production**: `npm run build` - Optimized bundle\n3. **Preview**: `npm run preview` - Preview production build\n\n### Contribution Guidelines\n1. Create a feature branch from `main`\n2. Make your changes\n3. Run `npm run lint` and `npm run format`\n4. Ensure tests pass (when configured)\n5. Submit a pull request\n\n## 5. Key Concepts\n\n### State Management (Pinia)\n\n**User Store** - Manages user authentication and profile:\n```javascript\nconst useUserStore = defineStore('user', () => {\n  const user = reactive({ role: null, token: null, name: null, id: null })\n  \n  const Login = async (login, pass) => { /* ... */ }\n  const getUser = async () => { /* ... */ }\n  const userExit = () => { /* ... */ }\n})\n```\n\n### API Layer\n\n**Axios Client** - Configured instance with interceptors:\n```javascript\nimport axios from 'axios'\nconst apiClient = axios.create({\n  baseURL: import.meta.env.VITE_SERVER_HOST,\n  headers: {\n    'Content-Type': 'application/json',\n  },\n})\n```\n\n### Authentication Flow\n1. User logs in via `LoginModel.vue`\n2. `Login()` method in user store calls `/auth/loginUser`\n3. Token is stored in localStorage\n4. `getUser()` fetches user profile\n5. Token is attached to API requests via interceptor\n\n### Routing\n- Uses Vue Router 4\n- Routes defined in `src/router/index.js`\n- Protected routes use middleware\n\n## 6. Common Tasks\n\n### Adding a New View\n\n1. Create view component:\n```bash\nsrc/views/MyView.vue\n```\n\n2. Add route:\n```javascript\n// src/router/index.js\n{ path: '/my-view', name: 'MyView', component: () => import('@/views/MyView.vue') }\n```\n\n3. Add navigation link:\n```vue\n<!-- In LayoutView.vue -->\n<RouterLink to=\"/my-view\">My View</RouterLink>\n```\n\n### Adding a New Store\n\n1. Create store file:\n```bash\nsrc/stores/myStore.js\n```\n\n2. Define store:\n```javascript\nimport { defineStore } from 'pinia'\n\nexport const useMyStore = defineStore('myStore', () => {\n  const state = ref({})\n  \n  const method = () => {}\n  \n  return { state, method }\n})\n```\n\n### Adding a New API Endpoint\n\n1. Create endpoint file:\n```bash\nsrc/api/myEndpoint.js\n```\n\n2. Define endpoint:\n```javascript\nimport apiClient from './client'\n\nexport const myApi = {\n  getData: () => apiClient.get('/endpoint')\n}\n```\n\n### Debugging Tips\n\n1. Use `debug()` utility from `@/utils/debug`\n2. Check browser DevTools Network tab for API calls\n3. Enable Vite dev server debug logging\n4. Use Vue DevTools extension\n\n## 7. Troubleshooting\n\n### Common Issues\n\n#### Import Resolution Errors\n```\nFailed to resolve import \"../component.vue\"\n```\n**Solution**: Use relative path from `src/` root:\n```javascript\nimport Component from '@/components/Component.vue'\n```\n\n#### Pinia Store Not Found\n```\nCannot read property 'user' of undefined\n```\n**Solution**: Import store correctly:\n```javascript\nimport { useUserStore } from '@/stores/user.js'\nconst store = useUserStore()\n```\n\n#### Vite Build Errors\n```\nModule not found: Error: Can't resolve\n```\n**Solution**: Check file exists and path is correct\n\n#### Authentication Errors\n```\n401 Unauthorized\n```\n**Solution**: Verify token is stored and sent in headers\n\n### Debug Logging\n```javascript\nimport { debug } from '@/utils/debug'\ndebug('Message:', data)\n```\n\n## 8. References\n\n### Documentation\n- [Vue 3 Documentation](https://vuejs.org/)\n- [Vite Documentation](https://vitejs.dev/)\n- [Pinia Documentation](https://pinia.vuejs.org/)\n- [Vue Router Documentation](https://router.vuejs.org/)\n- [Naive UI Documentation](https://www.naiveui.com/)\n- [Axios Documentation](https://axios-http.com/)\n\n### Project Files\n- `README.md` - Basic project overview\n- `openspec/specs/` - Technical specifications\n- `openspec/changes/` - Implementation plans\n\n### External Resources\n- [Vue 3 Composition API](https://vuejs.org/guide/extras/composition-api-faq.html)\n- [Vite Plugins](https://vitejs.dev/guide/api-plugin.html)\n- [Pinia Best Practices](https://pinia.vuejs.org/best-practices/)\n\n---\n\n**Last Updated**: $(date +%Y-%m-%d)\n**Author**: Development Team\n\n## Notes\n- This guide is auto-generated and should be updated as the project evolves\n- Additional rules.md files can be created in subdirectories for specific components\n- Keep this file in sync with actual project changes\n",