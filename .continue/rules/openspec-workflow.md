---
globs: "**/*.{md,txt}"
description: Standardize the OpenSpec workflow for proposing changes with
  complete artifacts in one step.
alwaysApply: false
---

When proposing changes, always generate three artifacts: proposal.md (what & why), design.md (how), and tasks.md (implementation steps).